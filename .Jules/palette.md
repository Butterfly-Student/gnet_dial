## 2026-05-21 - Vanilla JS for Interactions
**Learning:** The project had broken jQuery references in views (e.g., login page flash messages).
**Action:** Use vanilla JavaScript for simple DOM manipulations and interactions (like password toggles and fading elements) to avoid external library dependencies and reduce page weight.
## 2024-05-23 - Modern Dashboard & Auth UX
**Learning:** Adding a password toggle is a small but high-impact win for users frustrated by typos on mobile devices.
**Action:** Always include a show/hide password toggle on login forms, especially for mobile-first designs.

**Learning:** Consistent container width (e.g., ) across all views is critical for a polished feel when navigating between pages.
**Action:** Define a standard container class in the base layout or a utility class and use it everywhere.

**Learning:** Glassmorphism headers (backdrop-blur) add a significant modern touch without complex CSS.
**Action:** Use  for sticky headers to improve content readability while maintaining context.
## 2024-05-23 - Modern Dashboard & Auth UX
**Learning:** Adding a password toggle is a small but high-impact win for users frustrated by typos on mobile devices.
**Action:** Always include a show/hide password toggle on login forms, especially for mobile-first designs.

**Learning:** Consistent container width (e.g., `max-w-7xl`) across all views is critical for a polished feel when navigating between pages.
**Action:** Define a standard container class in the base layout or a utility class and use it everywhere.

**Learning:** Glassmorphism headers (backdrop-blur) add a significant modern touch without complex CSS.
**Action:** Use `bg-white/80 backdrop-blur-lg` for sticky headers to improve content readability while maintaining context.
